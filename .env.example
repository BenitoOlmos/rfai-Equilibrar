# ============================================================================
# CONFIGURACIÓN DE ENTORNO - SISTEMA RFAI
# Archivo de ejemplo para configuración local con XAMPP
# ============================================================================

# ----------------------------------------------------------------------------
# BASE DE DATOS - MySQL/MariaDB (XAMPP)
# ----------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=reprogramacion_foca

# Pool de Conexiones (Optimizado para XAMPP)
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0
DB_WAIT_FOR_CONNECTIONS=true
DB_ENABLE_KEEP_ALIVE=true
DB_KEEP_ALIVE_INITIAL_DELAY=10000

# ----------------------------------------------------------------------------
# SERVIDOR BACKEND
# ----------------------------------------------------------------------------
PORT=3005
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# ----------------------------------------------------------------------------
# AUTENTICACIÓN Y SEGURIDAD
# ----------------------------------------------------------------------------
JWT_SECRET=CAMBIAR_ESTO_EN_PRODUCCION_usar_cadena_aleatoria_muy_larga
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=10

# Session Secret (para Express Session si se usa)
SESSION_SECRET=CAMBIAR_ESTO_TAMBIEN_cadena_aleatoria_segura

# ----------------------------------------------------------------------------
# RUTAS DE RECURSOS
# ----------------------------------------------------------------------------
# Directorio donde se guardan audios, documentos, etc.
UPLOADS_DIR=./uploads
PUBLIC_URL=http://localhost:3005/public

# Tamaño máximo de archivos (en MB)
MAX_FILE_SIZE_MB=50

# ----------------------------------------------------------------------------
# CONFIGURACIÓN DE EMAIL (Para notificaciones)
# ----------------------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_email@gmail.com
SMTP_PASSWORD=tu_app_password
EMAIL_FROM=Clínica Equilibrar <noreply@equilibrar.cl>

# ----------------------------------------------------------------------------
# ANALÍTICA Y LOGS
# ----------------------------------------------------------------------------
# Intervalo de heartbeat para tracking de audios (milisegundos)
AUDIO_HEARTBEAT_INTERVAL=30000

# Nivel de logging (error, warn, info, debug)
LOG_LEVEL=debug

# ----------------------------------------------------------------------------
# CLOUDINARY (Si se usa para almacenamiento de archivos)
# ----------------------------------------------------------------------------
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ----------------------------------------------------------------------------
# GOOGLE MEET / CALENDAR (Si se integra)
# ----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3005/auth/google/callback

# ----------------------------------------------------------------------------
# FRONTEND
# ----------------------------------------------------------------------------
VITE_API_URL=http://localhost:3005/api
VITE_APP_NAME=RFAI - Clínica Equilibrar

# ----------------------------------------------------------------------------
# OBSERVACIONES IMPORTANTES
# ----------------------------------------------------------------------------
# 1. Este archivo NO debe subirse a Git (.gitignore)
# 2. Copiar este archivo como .env y completar con valores reales
# 3. Para producción, usar variables de entorno del servidor/cloud
# 4. JWT_SECRET debe ser una cadena aleatoria de al menos 64 caracteres
# 5. En XAMPP, DB_PASSWORD suele estar vacío por defecto
